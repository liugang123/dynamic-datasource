# 服务端口
server:
  port: 10002

# 数据源
spring:
  datasource:
    url: jdbc:mysql://xxxxxx:xxxxx/xxxx?useSSL=false
    driver-class-name: com.mysql.jdbc.Driver
    username: xxxxxx
    password: xxxxxx

mybatis:
  mapper-locations: classpath:com/example/dao/**.xml

# 请求上下文、动态数据源
com:
  example:
    filter:
      basicRequestContextFilter:
        enable: true
    dynamic:
      datasource:








